# G6 Platform Environment Variables Template
# Generated: 2025-09-26
# Purpose: Provide a curated, opinionated starting point for local development / staging.
# Notes:
#  * Only include vars that are commonly toggled. Exotic or legacy/deprecated flags are intentionally omitted.
#  * Commented defaults reflect current recommended values for a development workstation.
#  * Remove leading '#' to activate / override. Empty assignment means "set but empty"; omit variable entirely to use internal default.
#  * For exhaustive descriptions see docs/env_dict.md.
#  * Security-sensitive values (API keys, passwords) are NOT included—inject those via a secure secret manager or separate untracked .env.local.

############################
# CORE LOOP / TIMING
############################
# G6_CYCLE_INTERVAL=60              # Seconds per collection cycle (override config)
# G6_CYCLE_SLA_FRACTION=0.85        # SLA fraction threshold for breach metric
# G6_MAX_CYCLES=0                   # 0 = run indefinitely

############################
# INDEX / PARALLEL COLLECTION
############################
# G6_PARALLEL_INDICES=1             # Enable parallel per-index collectors
# G6_PARALLEL_INDEX_WORKERS=4       # Max threads for parallel collection
# G6_PARALLEL_INDEX_TIMEOUT_SEC=15  # Soft timeout per index task
# G6_PARALLEL_CYCLE_BUDGET_FRACTION=0.9

############################
# EXPIRY SERVICE & MISCLASS DETECTION
############################
# G6_EXPIRY_SERVICE=1               # Use centralized expiry classification
# G6_WEEKLY_EXPIRY_DOW=3            # Default weekly expiry weekday (Thu)
# G6_MONTHLY_EXPIRY_DOW=3           # Monthly anchor weekday
# G6_EXPIRY_MISCLASS_DETECT=1       # Enable misclassification instrumentation
# G6_EXPIRY_MISCLASS_DEBUG=0        # Verbose anomaly logs
# G6_EXPIRY_MISCLASS_SKIP=0         # Skip mismatching rows (set 1 to quarantine via omission)

############################
# CSV SINK / JUNK FILTER
############################
# G6_CSV_BASE_DIR=data/g6_data      # Alternate base directory for CSV outputs
# G6_CSV_JUNK_ENABLE=auto           # auto|on|off junk filtering mode
# G6_CSV_JUNK_MIN_TOTAL_OI=0        # Combined OI floor (enable >0)
# G6_CSV_JUNK_MIN_TOTAL_VOL=0       # Combined volume floor (enable >0)
# G6_CSV_JUNK_MIN_LEG_OI=0          # Per-leg OI floor
# G6_CSV_JUNK_MIN_LEG_VOL=0         # Per-leg volume floor
# G6_CSV_JUNK_STALE_THRESHOLD=0     # Identical price repetition threshold (0=off)
# G6_CSV_JUNK_WHITELIST=            # Patterns: INDEX:*, *:expiry_code, *
# G6_CSV_JUNK_SUMMARY_INTERVAL=0    # Seconds between summary log lines (0=off)
# G6_CSV_JUNK_DEBUG=0               # Debug logging for junk decisions

############################
# METRICS & CARDINALITY
############################
# G6_METRICS_CARD_ENABLED=1         # Master switch per-option metrics gating features
# G6_METRICS_CARD_ATM_WINDOW=0      # +/- steps around ATM (0=disabled)
# G6_CARDINALITY_MAX_SERIES=0       # Hard cap (0=unlimited)
# G6_CARDINALITY_REENABLE_FRACTION=0.7
# G6_CARDINALITY_MIN_DISABLE_SECONDS=300

############################
# ADAPTIVE STRIKE SCALING
############################
# G6_ADAPTIVE_STRIKE_SCALING=0      # Enable adaptive strike count scaling
# G6_ADAPTIVE_STRIKE_BREACH_THRESHOLD=3
# G6_ADAPTIVE_STRIKE_RESTORE_HEALTHY=10
# G6_ADAPTIVE_STRIKE_REDUCTION=0.8
# G6_ADAPTIVE_STRIKE_MIN=            # Hard lower bound

############################
# GREEKS / IV
############################
# G6_COMPUTE_GREEKS=0               # Enable Greeks calculation
# G6_ESTIMATE_IV=0                  # Enable IV solver
# G6_FORCE_GREEKS=0                 # Force on even if gating would disable
# G6_DISABLE_GREEKS=0               # Force off

############################
# LOGGING / CONSOLE
############################
# G6_OUTPUT_LEVEL=INFO              # Global log level
# G6_CONCISE_LOGS=1                 # Reduce verbosity
# G6_VERBOSE_CONSOLE=0
# G6_FANCY_CONSOLE=1                # Enable rich console if available
# G6_COLOR=auto                     # auto|always|never
# G6_JSON_LOGS=0                    # Structured JSON lines
# G6_LOG_FILE=logs/g6_platform.log

############################
# PANELS / SUMMARY UI
############################
# G6_OUTPUT_SINKS=csv,logging,panels
# G6_PANELS_DIR=data/panels
# G6_ENABLE_PANEL_PUBLISH=0

############################
# RUNTIME STATUS / CATALOG
############################
# G6_STATUS_FILE=data/runtime_status.json
# G6_EMIT_CATALOG=0
# G6_CATALOG_INTEGRITY=0

############################
# MEMORY PRESSURE (TUNING)
############################
# G6_MEMORY_LEVEL1_MB=200
# G6_MEMORY_LEVEL2_MB=300
# G6_MEMORY_LEVEL3_MB=500
# G6_MEMORY_GC_INTERVAL_SEC=0
# G6_MEMORY_MINOR_GC_EACH_CYCLE=0

############################
# RETRY / CIRCUIT BREAKER
############################
# G6_RETRY_PROVIDERS=0
# G6_RETRY_MAX_ATTEMPTS=0
# G6_RETRY_MAX_SECONDS=0
# G6_CB_FAILURES=5
# G6_CB_MIN_RESET=5
# G6_CB_MAX_RESET=30
# G6_CB_BACKOFF=5
# G6_CB_JITTER=0.1

############################
# PERFORMANCE TEST / OPTIONAL SUITES
############################
# G6_ENABLE_OPTIONAL_TESTS=0        # Run optional tests
# G6_ENABLE_PERF_TESTS=0            # Enable perf micro-benchmarks
# G6_ENABLE_SLOW_TESTS=0            # Enable slow tests

############################
# DATA QUALITY / GUARDS
############################
# G6_SKIP_ZERO_ROWS=0               # Skip rows with all zero metrics
# G6_PRICE_MAX_INDEX_FRAC=0.35
# G6_PRICE_MAX_STRIKE_FRAC=0.35

############################
# EXPERIMENTAL LOOP / BOOTSTRAP
############################
# G6_NEW_BOOTSTRAP=0
# G6_NEW_LOOP=0
# G6_LOOP_MARKET_HOURS=0

############################
# MISC / IDENTIFICATION
############################
# G6_RUN_ID=                        # Override generated run id
# G6_VERSION=
# G6_GIT_COMMIT=

############################
# SECURITY / AUTH PLACEHOLDERS (UNSET BY DEFAULT)
############################
# G6_HTTP_BASIC_USER=
# G6_HTTP_BASIC_PASS=
# G6_PROVIDERS_PRIMARY_API_KEY=

############################
# KITE CONNECT AUTH (Populate for live data collection)
############################
# KITE_API_KEY=
# KITE_API_SECRET=
# KITE_ACCESS_TOKEN=              # Leave blank to auto-acquire at startup (browser flow)
# KITE_REDIRECT_HOST=127.0.0.1    # Override only if custom redirect registered
# KITE_REDIRECT_PORT=5000
# KITE_REDIRECT_PATH=success
# KITE_REQUEST_TOKEN=             # Rare manual fallback (manual exchange)

############################
# LOCAL DEV UTILITIES (Task Runner)
############################
# G6_RUFF_BIN=                      # Path to ruff if not on PATH
# G6_MYPY_BIN=
# G6_PYTEST_BIN=
# G6_ENABLE_BLACK=0

############################
# GRAFANA / PROM / OVERLAY ENDPOINTS
############################
# Grafana picks up provisioning files from grafana/provisioning
# Set dashboard folder name and path if running Grafana service elsewhere
# G6_GRAFANA_FOLDER_NAME=G6 Platform
# G6_GRAFANA_DASH_PATH=C:\\Users\\ASUS\\Documents\\G6\\qq\\g6_reorganized\\grafana\\dashboards

# IMPORTANT: The following GF_PATHS_* variables are read by grafana-server and grafana-cli,
# not by Python. Keeping them here documents the values, but they only take effect if you
# export them into the Windows environment (system/user) or set them in the shell before
# launching Grafana. Use scripts/grafana_env_setup.ps1 to apply/persist them.
# GF_PATHS_HOME=C:\\Program Files\\GrafanaLabs\\grafana
# GF_PATHS_PLUGINS=C:\\Program Files\\GrafanaLabs\\grafana\\data\\plugins
# GF_PATHS_PROVISIONING=C:\\Users\\ASUS\\Documents\\G6\\qq\\g6_reorganized\\grafana\\provisioning

# Prometheus URL (used by grafana provisioning); defaults to local 9090
# G6_PROM_URL=http://127.0.0.1:9090

# Overlay API base (used in docs/workflows; dashboard uses explicit URL 127.0.0.1:9500)
# G6_OVERLAY_API=http://127.0.0.1:9500

############################
# OVERRIDE FOR TESTING ONE-OFFS (USE SPARINGLY)
############################
# G6_FORCE_MARKET_OPEN=0
# G6_PROCESS_START_TS=              # Epoch seconds (time travel)
# G6_EXPIRY_MISCLASS_SKIP=0         # Repeated here for clarity

# End of template
