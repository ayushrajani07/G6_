"""Historical legacy panels bridge tests.

Converted to no-ops after full removal of the legacy panels bridge script.
Retained only as a stub to avoid import errors in any downstream runs referencing
these test names (will be deleted in a later cleanup pass once CI history settles).
"""

from __future__ import annotations

def test_panels_bridge_deprecation_stub():
    # No-op: legacy bridge removed.
    assert True